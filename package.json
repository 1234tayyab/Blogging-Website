{
  "name": "blogging-website",
  "version": "1.0.0",
  "description": "A blogging website with HTML/CSS linting and Parcel build",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "htmlhint": "^1.1.5",
    "stylelint": "^15.10.3",
    "parcel": "^2.10.0"
  }
}
